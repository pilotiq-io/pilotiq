# Build output
dist/

# Dependencies
node_modules/

# Environment
.env
.env.*
!.env.example

# Forge CLI state (color rotation persistence)
.forge-cli-state.json

# Cache & tooling
.turbo/
*.log
dev.db
bootstrap/cache/

# Auto-generated by @pilotiq/pilotiq's Vite plugin — written at vite
# construction time on `pnpm dev`/`build` and by the `pretypecheck`
# regen script. Don't commit; the templates live in vite.ts.
pages/(pilotiq)/

# Storage uploads
storage/app/
public/storage

# Playwright
test-results/
