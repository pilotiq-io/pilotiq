---
"@pilotiq/pilotiq": patch
---

Underline-variant `Tabs` list now spans the full available width so the bottom border runs across the content column; triggers stay compact at the start (`flex-none`).
